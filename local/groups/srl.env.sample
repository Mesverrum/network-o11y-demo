# DEPRECATED — use groups/srl-hq.env.sample (AWS-aligned HQ group).
#
#   cp groups/srl-hq.env.sample groups/srl-hq.env
#   make generate && make up
#
# Legacy name `srl` is migrated automatically by scripts/ensure-snmp-groups.sh
# when groups/srl.env exists and groups/srl-hq.env does not.
